# وضعیت فعلی پروژه ArtimanLeads

**تاریخ:** 2026-08-06
**فاز:** Stabilization (تثبیت زیرساخت)

---

## ✅ Task جاری (انجام شد)

### **عنوان:** اصلاح Route Backup در Navigation

**وضعیت:** ✅ انجام شد

**تغییرات اعمال‌شده:**
- در `resources/views/layouts/navigation.blade.php`:
  - `route('backup.index')` به `route('admin.backup')` تغییر کرد.

**نتیجه:**
- لینک Backup در Navigation به مسیر صحیح `/admin/backup` هدایت می‌شود.
- Route `backup.index` دیگر در Navigation استفاده نمی‌شود.

---

## 📊 وضعیت ماژول‌ها

| ماژول | وضعیت | مشکل |
|-------|-------|------|
| **Dashboard** | ✅ درست کار می‌کند | - |
| **Navigation** | ✅ اصلاح شد | - |
| **Project** | ✅ درست کار می‌کند | - |
| **Assignment** | ✅ درست کار می‌کند | - |
| **Call Log** | ✅ درست کار می‌کند | - |
| **User Management** | ✅ درست کار می‌کند | - |
| **Backup** | ✅ درست کار می‌کند | - |

---

## 📋 برنامه مرحله بعدی

### **اولویت بعدی (P2): Refactor مدل Project**

**مشکل:**
- فیلدهای تکراری مانند `purchase_status`, `project_level`, `level` در مدل Project وجود دارند.
- برخی فیلدها در Controller و Model دوبارگی دارند.

**اقدام مورد نیاز:**
- بررسی دقیق فیلدهای مدل Project.
- شناسایی فیلدهای اضافی و تکراری.
- ارائه پیشنهاد برای Refactor.

---

## 📝 قوانین توسعه

1. قبل از هر تغییر، `docs/AI_CONTEXT.md` و `docs/CURRENT_TASK.md` خوانده شوند.
2. تغییرات فقط برای Task جاری انجام شود.
3. بعد از هر تغییر، `CURRENT_TASK.md` به‌روزرسانی شود.
4. هیچ دستور `npm` روی سرور اجرا نشود.
5. فایل‌های `public/build/` حذف نشوند.
6. قبل از تغییرات مخرب (مثل `rm`)، توقف و تأیید گرفته شود.

---

## 📎 اطلاعات پروژه

- **مسیر سرور:** `/home/cp36529/public_html/artimanleads`
- **نسخه Laravel:** 11
- **نسخه PHP:** 8.2
- **محیط:** cPanel (Production)
- **Branch:** master

---

## 🔴 پروتکل کنترل تغییرات پروژه

این پروتکل در `docs/AI_CONTEXT.md` ثبت شده است و تمام Taskهای آینده باید از آن پیروی کنند.

**قوانین کلیدی:**
1. ابتدا بررسی، سپس تحلیل، سپس پیشنهاد، سپس تأیید ساجد، سپس اجرا.
2. بدون تأیید صریح ساجد، هیچ تغییری انجام نمی‌شود.
3. تمام Commitها با گزارش کامل `git status`, `git diff` انجام می‌شوند.
4. هیچ تغییری خارج از Scope Task انجام نمی‌شود.
